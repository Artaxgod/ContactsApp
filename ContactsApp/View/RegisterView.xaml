<Window x:Class="ContactsApp.View.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactsApp.View"
        mc:Ignorable="d"
        Title="Регистрация" Height="350" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Фамилия:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="LastNameTextBox" Margin="0,0,0,10" Height="30"/>

            <TextBlock Text="Имя:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="FirstNameTextBox" Margin="0,0,0,10" Height="30"/>

            <TextBlock Text="Email:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="EmailTextBox" Margin="0,0,0,10" Height="30"/>

            <TextBlock Text="Телефон:" Margin="0,0,0,5" FontWeight="Bold"/>
            <TextBox x:Name="PhoneTextBox" Margin="0,0,0,20" Height="30"/>

            <Button Content="Зарегистрироваться" Click="RegisterButton_Click" Height="30"/>

            <TextBlock x:Name="ErrorTextBlock" Foreground="Red" Margin="0,10,0,0" 
                       TextWrapping="Wrap" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
